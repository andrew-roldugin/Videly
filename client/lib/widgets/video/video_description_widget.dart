import 'package:flutter/widgets.dart';

class VideoDescriptionWidget extends StatelessWidget {
  const VideoDescriptionWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
